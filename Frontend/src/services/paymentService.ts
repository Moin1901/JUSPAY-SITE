// import axios from "axios";

// const API_BASE = "http://localhost:5000/api/payment";

// export const createOrder = async (amount) => {
//   const response = await axios.post(`${API_BASE}/create-order`, { amount });
//   return response.data;
// };

// export const verifyPayment = async (paymentIntentId) => {
//   const response = await axios.post(`${API_BASE}/verify-payment`, {
//     paymentIntentId,
//   });
//   return response.data;
// };
